import { EmailPreviewDemo } from "@/components/EmailPreview";

export const metadata = {
  title: "Aperçu Email — Respirfacile",
};

export default function EmailPreviewPage() {
  return <EmailPreviewDemo />;
}
